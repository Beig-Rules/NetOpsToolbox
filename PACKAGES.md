# Packages

## npm (GitHub Packages)

```bash
npm install @beig-rules/netops-toolbox --registry=https://npm.pkg.github.com
```

Requires a GitHub personal access token with `read:packages` in `~/.npmrc`.

## Preview site

After Pages is enabled (Settings → Pages → GitHub Actions):

https://beig-rules.github.io/NetOpsToolbox/

Until then use:

https://cdn.jsdelivr.net/gh/Beig-Rules/NetOpsToolbox@main/index.html
